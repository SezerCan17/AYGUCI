<UserControl x:Class="AYGUCI.Views.IHAListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PanelBG}">

    <Border
            BorderBrush="{StaticResource BorderColor}"
            BorderThickness="1"
            CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<Grid 
				Grid.Row="0"
				Margin="15,15,15,10">
				
				<TextBlock Text="İHA LİSTESİ" Foreground="{StaticResource AccentBlue}" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                <TextBlock Text="&#xE8CB;" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource TextDim}" FontSize="14" HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="Hand"/>
                
			</Grid>

            <Border Grid.Row="1" Background="{StaticResource PrimaryBG}" BorderBrush="{StaticResource PanelBG}" BorderThickness="1" CornerRadius="4" Margin="15,0,15,10" Height="30">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="&#xE71E;" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource TextDim}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextMain}" FontSize="12" VerticalAlignment="Center" Text="Ara..." Opacity="0.5" />

                    <TextBlock Grid.Column="2" Text="&#xE71C;" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource TextDim}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Cursor="Hand"/>

                </Grid>
                
                
                
            </Border>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding IHAList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="15,10" Cursor="Hand" Background="{StaticResource BorderColor}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="60"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding UavId}" Foreground="{StaticResource TextMain}" FontSize="11" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Callsign}" Foreground="{StaticResource TextDim}" FontSize="9"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,0">
                                        <TextBlock Text="{Binding BatteryPercentage, StringFormat='%\{0\}'}" 
                                       Foreground="{Binding StatusColorBrush}" FontSize="11" FontWeight="SemiBold" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding BatteryIcon}" 
                                       FontFamily="Segoe MDL2 Assets" 
                                       Foreground="{Binding StatusColorBrush}" FontSize="14"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="3" Text="{Binding AltitudeInfo}" 
                                   Foreground="{Binding AltitudeColorBrush}" 
                                   FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right"/>

                                </Grid>
                                
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    
                </ItemsControl>
                
            </ScrollViewer>

            <Border Grid.Row="3" Margin="15" Background="{StaticResource PrimaryBG}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4">
                <Button Background="Transparent" BorderThickness="0" Height="35" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" Foreground="{StaticResource TextDim}" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="YENİ İHA EKLE" Foreground="{StaticResource TextDim}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Border>


        </Grid>
    </Border>
    
</UserControl>